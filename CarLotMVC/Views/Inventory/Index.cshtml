@using AutoLotDAL.Models
@model IEnumerable<AutoLotDAL.Models.Inventory>
@helper ShowInventory(Inventory item)
{
@item.Make<text></text>
@item.Color<text></text>
@item.PetName<text></text>
    }
@{
    ViewBag.Title = "Index";
}

@functions
{
    public IList<Inventory> SortCar(IList<Inventory> cars)
    {
        var list = from s in cars orderby s.PetName select s;
        return list.ToList();

    }
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Make)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Color)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PetName)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in SortCar(Model.ToList()) ){
    @Html.DisplayFor(modelItem => item,"InventoryList")
    @*<tr>
        <td>
            @Html.DisplayFor(modelItem => item.Make)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Color)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PetName)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>*@
    }
}

</table>
